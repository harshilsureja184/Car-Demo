import { Component } from '@angular/core';
import { RouterLink, RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-cng',
  imports: [RouterLink , RouterOutlet],
  templateUrl: './cng.html',
  styleUrl: './cng.css',
})
export class Cng {

}
